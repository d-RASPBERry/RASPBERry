# Seed overlay for Ape-X DDQN: Ape-X DDQN Pong D-PER seed 0
extends: ../../../apex/per/pong.yml

env_config:
  env_alias: "APEX-PER-Pong-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "Ape-X DDQN"
    seed: "0"
