# Seed overlay for Ape-X DDQN: Ape-X DDQN Atlantis D-PBER seed 0
extends: ../../../apex/pber/atlantis.yml

env_config:
  env_alias: "APEX-PBER-Atlantis-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "Ape-X DDQN"
    seed: "0"
