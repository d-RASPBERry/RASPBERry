# Seed overlay for Ape-X DDQN: Ape-X DDQN Atlantis D-PBER seed 4
extends: ../../../apex/pber/atlantis.yml

env_config:
  env_alias: "APEX-PBER-Atlantis-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "Ape-X DDQN"
    seed: "4"
