# Seed overlay for SAC: SAC CarRacing RASPBERry seed 1
extends: ../../../sac/raspberry/carracing.yml

env_config:
  env_alias: "SAC-RASPBERry-CarRacing-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
