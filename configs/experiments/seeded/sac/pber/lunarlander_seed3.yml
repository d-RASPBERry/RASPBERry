# Seed overlay for SAC: SAC LunarLander PBER seed 3
extends: ../../../sac/pber/lunarlander.yml

env_config:
  env_alias: "SAC-PBER-LunarLander-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "3"
