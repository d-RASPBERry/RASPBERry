# Seed overlay for SAC: SAC Walker2d PBER seed 2
extends: ../../../sac/pber/walker2d_state.yml

env_config:
  env_alias: "SAC-PBER-Walker2d-State-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "2"
