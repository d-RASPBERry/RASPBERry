# Seed overlay for SAC: SAC Walker2d PBER seed 1
extends: ../../../sac/pber/walker2d_state.yml

env_config:
  env_alias: "SAC-PBER-Walker2d-State-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
