# Seed overlay for SAC: SAC CarRacing PBER seed 4
extends: ../../../sac/pber/carracing.yml

env_config:
  env_alias: "SAC-PBER-CarRacing-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "4"
