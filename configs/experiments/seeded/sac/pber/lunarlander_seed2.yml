# Seed overlay for SAC: SAC LunarLander PBER seed 2
extends: ../../../sac/pber/lunarlander.yml

env_config:
  env_alias: "SAC-PBER-LunarLander-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "2"
