# Seed overlay for SAC: SAC CarRacing PBER seed 2
extends: ../../../sac/pber/carracing.yml

env_config:
  env_alias: "SAC-PBER-CarRacing-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "2"
