# Seed overlay for SAC: SAC Walker2d PBER seed 0
extends: ../../../sac/pber/walker2d_state.yml

env_config:
  env_alias: "SAC-PBER-Walker2d-State-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "0"
