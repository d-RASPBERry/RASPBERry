# Seed overlay for SAC: SAC LunarLander PBER seed 1
extends: ../../../sac/pber/lunarlander.yml

env_config:
  env_alias: "SAC-PBER-LunarLander-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
