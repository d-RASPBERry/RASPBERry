# Seed overlay for SAC: SAC CarRacing PBER seed 3
extends: ../../../sac/pber/carracing.yml

env_config:
  env_alias: "SAC-PBER-CarRacing-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "3"
