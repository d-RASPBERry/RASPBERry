# Seed overlay for SAC: SAC LunarLander PBER seed 4
extends: ../../../sac/pber/lunarlander.yml

env_config:
  env_alias: "SAC-PBER-LunarLander-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "4"
