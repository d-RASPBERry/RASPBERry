# Seed overlay for SAC: SAC LunarLander PBER seed 0
extends: ../../../sac/pber/lunarlander.yml

env_config:
  env_alias: "SAC-PBER-LunarLander-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "0"
