# Seed overlay for SAC: SAC CarRacing PBER seed 1
extends: ../../../sac/pber/carracing.yml

env_config:
  env_alias: "SAC-PBER-CarRacing-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
