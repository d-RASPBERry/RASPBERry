# Seed overlay for SAC: SAC LunarLander PER seed 1
extends: ../../../sac/per/lunarlander.yml

env_config:
  env_alias: "SAC-PER-LunarLander-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
