# Seed overlay for SAC: SAC Walker2d PER seed 2
extends: ../../../sac/per/walker2d_state.yml

env_config:
  env_alias: "SAC-PER-Walker2d-State-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "2"
