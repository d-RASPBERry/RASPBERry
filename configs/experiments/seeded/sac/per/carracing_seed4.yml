# Seed overlay for SAC: SAC CarRacing PER seed 4
extends: ../../../sac/per/carracing.yml

env_config:
  env_alias: "SAC-PER-CarRacing-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "4"
