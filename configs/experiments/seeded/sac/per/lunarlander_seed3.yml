# Seed overlay for SAC: SAC LunarLander PER seed 3
extends: ../../../sac/per/lunarlander.yml

env_config:
  env_alias: "SAC-PER-LunarLander-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "3"
