# Seed overlay for SAC: SAC LunarLander PER seed 0
extends: ../../../sac/per/lunarlander.yml

env_config:
  env_alias: "SAC-PER-LunarLander-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "0"
