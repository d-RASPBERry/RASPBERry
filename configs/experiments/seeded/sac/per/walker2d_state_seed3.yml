# Seed overlay for SAC: SAC Walker2d PER seed 3
extends: ../../../sac/per/walker2d_state.yml

env_config:
  env_alias: "SAC-PER-Walker2d-State-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "3"
