# Seed overlay for SAC: SAC CarRacing PER seed 1
extends: ../../../sac/per/carracing.yml

env_config:
  env_alias: "SAC-PER-CarRacing-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "1"
