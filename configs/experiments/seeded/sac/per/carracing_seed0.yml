# Seed overlay for SAC: SAC CarRacing PER seed 0
extends: ../../../sac/per/carracing.yml

env_config:
  env_alias: "SAC-PER-CarRacing-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "0"
