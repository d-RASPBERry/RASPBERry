# Seed overlay for SAC: SAC CarRacing PER seed 2
extends: ../../../sac/per/carracing.yml

env_config:
  env_alias: "SAC-PER-CarRacing-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "2"
