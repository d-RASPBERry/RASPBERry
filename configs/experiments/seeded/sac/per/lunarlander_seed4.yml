# Seed overlay for SAC: SAC LunarLander PER seed 4
extends: ../../../sac/per/lunarlander.yml

env_config:
  env_alias: "SAC-PER-LunarLander-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "SAC"
    seed: "4"
