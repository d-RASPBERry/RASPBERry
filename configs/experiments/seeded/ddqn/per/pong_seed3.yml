# Seed overlay for DDQN: DDQN Pong PER seed 3
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "DDQN-PER-Pong-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "3"
