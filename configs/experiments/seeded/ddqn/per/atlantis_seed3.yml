# Seed overlay for DDQN: DDQN Atlantis PER seed 3
extends: ../../../ddqn/per/atlantis.yml

env_config:
  env_alias: "DDQN-PER-Atlantis-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "3"
