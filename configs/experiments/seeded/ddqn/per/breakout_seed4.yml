# Seed overlay for DDQN: DDQN Breakout PER seed 4
extends: ../../../ddqn/per/breakout.yml

env_config:
  env_alias: "DDQN-PER-Breakout-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
