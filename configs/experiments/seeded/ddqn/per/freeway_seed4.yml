# Seed overlay for DDQN: DDQN Freeway PER seed 4
extends: ../../../ddqn/per/freeway.yml

env_config:
  env_alias: "DDQN-PER-Freeway-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
