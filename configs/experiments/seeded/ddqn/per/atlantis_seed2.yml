# Seed overlay for DDQN: DDQN Atlantis PER seed 2
extends: ../../../ddqn/per/atlantis.yml

env_config:
  env_alias: "DDQN-PER-Atlantis-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
