# Seed overlay for DDQN: DDQN Pong PER seed 0
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "DDQN-PER-Pong-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "0"
