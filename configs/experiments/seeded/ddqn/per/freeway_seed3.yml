# Seed overlay for DDQN: DDQN Freeway PER seed 3
extends: ../../../ddqn/per/freeway.yml

env_config:
  env_alias: "DDQN-PER-Freeway-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "3"
