# Seed overlay for DDQN: DDQN Freeway PER seed 1
extends: ../../../ddqn/per/freeway.yml

env_config:
  env_alias: "DDQN-PER-Freeway-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
