# Seed overlay for DDQN: DDQN Pong PER seed 2
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "DDQN-PER-Pong-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
