# Seed overlay for DDQN: DDQN Atlantis PER seed 4
extends: ../../../ddqn/per/atlantis.yml

env_config:
  env_alias: "DDQN-PER-Atlantis-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
