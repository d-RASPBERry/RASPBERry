# Seed overlay for DDQN: DDQN Pong PER seed 1
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "DDQN-PER-Pong-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
