# Seed overlay for DDQN: DDQN Breakout PER seed 1
extends: ../../../ddqn/per/breakout.yml

env_config:
  env_alias: "DDQN-PER-Breakout-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
