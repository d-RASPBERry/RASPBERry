# Seed overlay for DDQN: DDQN Atlantis PER seed 1
extends: ../../../ddqn/per/atlantis.yml

env_config:
  env_alias: "DDQN-PER-Atlantis-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
