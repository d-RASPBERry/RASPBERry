# Seed overlay for DDQN: DDQN Breakout PER seed 2
extends: ../../../ddqn/per/breakout.yml

env_config:
  env_alias: "DDQN-PER-Breakout-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
