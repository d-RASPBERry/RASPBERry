# Seed overlay for DDQN: DDQN Pong PER seed 4
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "DDQN-PER-Pong-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
