# Seed overlay for DDQN: DDQN Pong PBER seed 0
extends: ../../../ddqn/pber/pong.yml

env_config:
  env_alias: "DDQN-PBER-Pong-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "0"
