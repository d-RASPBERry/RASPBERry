# Seed overlay for DDQN: DDQN Pong PBER seed 2
extends: ../../../ddqn/pber/pong.yml

env_config:
  env_alias: "DDQN-PBER-Pong-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
