# Seed overlay for DDQN: DDQN Freeway PBER seed 0
extends: ../../../ddqn/pber/freeway.yml

env_config:
  env_alias: "DDQN-PBER-Freeway-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "0"
