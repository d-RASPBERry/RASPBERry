# Seed overlay for DDQN: DDQN Freeway PBER seed 1
extends: ../../../ddqn/pber/freeway.yml

env_config:
  env_alias: "DDQN-PBER-Freeway-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
