# Seed overlay for DDQN: DDQN Freeway PBER seed 3
extends: ../../../ddqn/pber/freeway.yml

env_config:
  env_alias: "DDQN-PBER-Freeway-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "3"
