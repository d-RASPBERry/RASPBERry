# Seed overlay for DDQN: DDQN Breakout PBER seed 0
extends: ../../../ddqn/pber/breakout.yml

env_config:
  env_alias: "DDQN-PBER-Breakout-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "0"
