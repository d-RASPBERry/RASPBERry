# Seed overlay for DDQN: DDQN Atlantis PBER seed 1
extends: ../../../ddqn/pber/atlantis.yml

env_config:
  env_alias: "DDQN-PBER-Atlantis-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
