# Seed overlay for DDQN: DDQN Atlantis PBER seed 2
extends: ../../../ddqn/pber/atlantis.yml

env_config:
  env_alias: "DDQN-PBER-Atlantis-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
