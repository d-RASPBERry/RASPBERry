# Seed overlay for DDQN: DDQN Freeway PBER seed 2
extends: ../../../ddqn/pber/freeway.yml

env_config:
  env_alias: "DDQN-PBER-Freeway-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
