# Seed overlay for DDQN: DDQN Atlantis PBER seed 4
extends: ../../../ddqn/pber/atlantis.yml

env_config:
  env_alias: "DDQN-PBER-Atlantis-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
