# Seed overlay for DDQN: DDQN Pong PBER seed 4
extends: ../../../ddqn/pber/pong.yml

env_config:
  env_alias: "DDQN-PBER-Pong-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
