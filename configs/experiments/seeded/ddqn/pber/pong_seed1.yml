# Seed overlay for DDQN: DDQN Pong PBER seed 1
extends: ../../../ddqn/pber/pong.yml

env_config:
  env_alias: "DDQN-PBER-Pong-seed1"

hyper_parameters:
  seed: 1

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "1"
