# Seed overlay for DDQN: DDQN Breakout PBER seed 2
extends: ../../../ddqn/pber/breakout.yml

env_config:
  env_alias: "DDQN-PBER-Breakout-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "2"
