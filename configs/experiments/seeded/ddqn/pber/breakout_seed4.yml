# Seed overlay for DDQN: DDQN Breakout PBER seed 4
extends: ../../../ddqn/pber/breakout.yml

env_config:
  env_alias: "DDQN-PBER-Breakout-seed4"

hyper_parameters:
  seed: 4

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "4"
