# Seed overlay for DDQN: DDQN Atlantis PBER seed 3
extends: ../../../ddqn/pber/atlantis.yml

env_config:
  env_alias: "DDQN-PBER-Atlantis-seed3"

hyper_parameters:
  seed: 3

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "3"
