# Seed overlay for DDQN: DDQN Atlantis PBER seed 0
extends: ../../../ddqn/pber/atlantis.yml

env_config:
  env_alias: "DDQN-PBER-Atlantis-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "DDQN"
    seed: "0"
