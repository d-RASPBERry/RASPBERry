# Seed overlay for Edge: DDQN Pong PER seed 0
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "Edge-DDQN-PER-Pong-seed0"

hyper_parameters:
  seed: 0

mlflow:
  tags:
    experiment_group: "Edge"
    seed: "0"
