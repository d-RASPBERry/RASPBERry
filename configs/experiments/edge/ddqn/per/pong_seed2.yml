# Seed overlay for Edge: DDQN Pong PER seed 2
extends: ../../../ddqn/per/pong.yml

env_config:
  env_alias: "Edge-DDQN-PER-Pong-seed2"

hyper_parameters:
  seed: 2

mlflow:
  tags:
    experiment_group: "Edge"
    seed: "2"
