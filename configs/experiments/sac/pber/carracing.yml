# SAC-PBER: BOX2DI-CarRacing (Image observation)
# PBER = Block Experience Replay without compression (Chapter 3: PBER)
extends: ../../../templates/sac_image_base.yml

env_config:
  env_name: "BOX2DI-CarRacing"
  env_alias: "SAC-PBER-CarRacing"
  normalize: true
  dtype: "float32"
  # SKIP_INITIAL_FRAMES handled in utils/__init__.py

logging:
  log_subdir: "BOX2DI/CarRacing-PBER"

mlflow:
  experiment: "SAC-CarRacing"
  tags:
    algorithm: "SAC"
    buffer: "PBER"
    environment: "CarRacing"
    obs_type: "image"

run_config:
  max_time_s: 1800

hyper_parameters:
  # Optimized for Image-based Continuous Control
  train_batch_size: 256
  grad_clip: 1.0
  n_step: 3
  rollout_fragment_length: 8
  
  # Optimization
  initial_alpha: 0.2
  optimization:
    actor_learning_rate: 0.0003
    critic_learning_rate: 0.0003
    entropy_learning_rate: 0.0003

  # Sampling & Training
  min_sample_timesteps_per_iteration: 10000
  training_intensity: 4.0
  target_entropy: -3.0

  # Replay buffer (PBER: Block-based without compression)
  replay_buffer_config:
    capacity: 200000
    type: MultiAgentPrioritizedBlockReplayBuffer
    sub_buffer_size: 32  # Transitions per block

  # Resource
  num_workers: 9
  num_cpus: 10
  num_envs_per_worker: 8
