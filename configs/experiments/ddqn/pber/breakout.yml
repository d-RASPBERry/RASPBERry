# DDQN-PBER: Atari-Breakout
# Environment creation check (image observations)
extends: ../../../templates/ddqn_base.yml

env_config:
  id: "Atari-BreakoutNoFrameskip-v4"
  env_alias: "DDQN-PBER-Breakout"

logging:
  log_subdir: "Atari/Breakout"
  log_every: 10

mlflow:
  experiment: "DDQN-Atari"
  tags:
    algorithm: "DDQN"
    buffer: "PBER"
    environment: "Breakout"
    obs_type: "image"

run_config:
  use_mlflow: true
  run_name_template: "{env_alias}"
  max_iterations: 100000
  max_time_s: 360000

hyper_parameters:
  # Replay buffer (PBER baseline)
  replay_buffer_config:
    capacity: 1000000
    prioritized_replay_alpha: 0.5
    prioritized_replay_beta: 1.0
    prioritized_replay_eps: 0.000001
    type: MultiAgentPrioritizedBlockReplayBuffer
    sub_buffer_size: 8

  training_intensity: 1.0

  # Keep core hyperparameters aligned with Pong baseline
  lr: 1.e-4
  gamma: 0.99
  num_workers: 2
  num_envs_per_worker: 8
