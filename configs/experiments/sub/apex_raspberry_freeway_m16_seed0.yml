# JR06 block-size ablation (APEX-RASPBERry)
# env=freeway, m=sub_buffer_size=16, seed=0
extends: configs/experiments/apex/raspberry/freeway.yml

env_config:
  env_alias: "APEX-RASPBERry-Freeway-m16-seed0"

hyper_parameters:
  seed: 0
  replay_buffer_config:
    sub_buffer_size: 16

mlflow:
  tags:
    ablation: "jr06_blocksize"
    block_size: "16"
    seed: "0"
