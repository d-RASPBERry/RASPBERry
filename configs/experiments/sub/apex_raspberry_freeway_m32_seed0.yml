# JR06 block-size ablation (APEX-RASPBERry)
# env=freeway, m=sub_buffer_size=32, seed=0
extends: ../apex/raspberry/freeway.yml

env_config:
  env_alias: "APEX-RASPBERry-Freeway-m32-seed0"

hyper_parameters:
  seed: 0
  replay_buffer_config:
    sub_buffer_size: 32

mlflow:
  tags:
    ablation: "jr06_blocksize"
    block_size: "32"
    seed: "0"
