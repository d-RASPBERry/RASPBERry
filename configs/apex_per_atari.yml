# APEX-DQN-PER configuration for Atari environments
extends: templates/apex_base.yml

hyper_parameters:
  training_intensity: 4.0
  # Replay buffer (RASPBERry)
  replay_buffer_config:
    capacity: 1000000
    prioritized_replay_alpha: 0.5
    prioritized_replay_beta: 1.0
    prioritized_replay_eps: 0.000001
    type: MultiAgentPrioritizedReplayBuffer
    replay_buffer_shards_colocated_with_driver: true
    worker_side_prioritization: true
    no_local_replay_buffer: true

mlflow:
  experiment: "APEX-Atari"
  tags:
    algorithm: "APEX-DQN"
    buffer: "PER"
    obs_type: "image"

run_config:
  use_mlflow: true
  run_name_template: "{env_in}_APEX_PER"
  max_iterations: 100000
  max_time_s: 360000
